figure {
  margin: 0.5rem 0;
}

figure figcaption {
  font-size: var(--step--1);
  font-style: italic;
  padding-left: var(--padding-base);
}

figure:has(img[src$='.gif']) figcaption {
  max-width: 50%;
}

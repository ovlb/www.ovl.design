/* Stats blocks (migrated from stats.vue). */

.block-stats {
  display: flex;
  flex-flow: row wrap;
  gap: var(--padding-base);
  justify-content: space-evenly;
  padding: var(--padding-base);
}

.block-stats li::marker {
  font-variant-numeric: oldstyle-nums;
}
